# Clinical Trial Analysis & Dashboard

## Overview

This project analyzes clinical trial data to understand how the drug candidate **miraclib** affects immune cell populations. The pipeline includes data ingestion, database design, statistical analysis, and an interactive dashboard.

The goal is to compare immune cell population frequencies between responders and non-responders and identify patterns that may predict treatment response.

---

## How to Run

All steps are automated using a Makefile:

```bash
make setup
make pipeline
make dashboard
```

* `make setup`: installs dependencies
* `make pipeline`: runs full analysis (data loading + outputs)
* `make dashboard`: launches interactive dashboard

---

## Project Structure

```
.
├── load_data.py    # Part 1: intialize database and load input csv
├── analysis.py     # Parts 2-4: analysis
├── dashboard.py    # Interactive Streamlit dashboard 
├── requirements.txt
├── Makefile
├── cell-count.csv  # Input csv 
├── output/         # Output files generated by `make pipeline`
└── README.md
```

---

## Database Schema

The SQLite database is normalized into three tables:

* **subjects**: patient-level metadata
* **samples**: sample-level information (timepoint, treatment, etc.)
* **cell_counts**: immune cell population counts

### Rationale

* Reduces redundancy
* Improves scalability for larger datasets. For example, new patient samples can be added to samples table without altering subjects table, additional analyses on existing samples can be added as new tables, and additional cell types can be added to cell counts.  

---

## Code Structure

* **load_data.py**
  Initializes the database schema and loads data from `cell-count.csv`.

* **analysis.py**
  Performs:

  * summary table generation (relative frequencies)
  * statistical analysis (responders vs non-responders)
  * visualization (boxplots)
  * subset queries (Part 4)

  Statistical methods:

  * Welch’s t-test (when n > 30)
  * Mann–Whitney U test (smaller samples)

* **dashboard.py**
  Interactive dashboard for exploring results.

---

## Analysis Summary

### Part 2: Data Overview

* Calculates total cell counts per sample
* Computes relative frequencies (%) for each population

### Part 3: Statistical Analysis

* Compares PBMC samples between responders and non-responders
* Identifies statistically significant differences
* Visualizes distributions using boxplots

### Part 4: Subset Analysis

* Filters melanoma PBMC baseline samples (time = 0, miraclib-treated)
* Reports:

  * samples per project
  * responder vs non-responder counts
  * gender distribution
  * average B cell count for male responders

---

## Key Findings

* CD4 T cell frequencies differed significantly between responders and non-responders (p < 0.05)
* However, the absolute difference (~0.6%) is small and may not be biologically meaningful

---

## Dashboard Link

To run locally:

```bash
make dashboard
```

Then open the provided local URL in your browser.

---

## Author

Helena Hopson
