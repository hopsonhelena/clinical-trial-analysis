# Clinical Trial Analysis & Dashboard

## Overview

This project analyzes clinical trial data and immune cell populations for Bob. The pipeline includes data upload, database design, statistical analysis, and an interactive dashboard.

---

## How to Run

All steps are automated using a Makefile:

* `make setup`: installs dependencies
* `make pipeline`: runs full analysis (data loading + outputs)
* `make dashboard`: launches interactive dashboard

---

## How to Access Dashboard

1. In Codespaces, look at the bottom panel    
2. Click “Ports” tab   
3. You should see something like:  
**Port** 8501   
4. Under **Forwarded Address**, click the globe button  

---

## Project Structure

```
.
├── load_data.py    # Part 1: intialize database and load input csv
├── analysis.py     # Parts 2-4: analysis
├── dashboard.py    # Interactive Streamlit dashboard 
├── requirements.txt
├── Makefile
├── cell-count.csv  # Input csv 
├── output/         # Output files generated by make pipeline
└── README.md
```


## Database Schema

The SQLite database is normalized into three tables:

* **subjects**: patient-level demographic data (sex, age)
* **samples**: sample-level information (timepoint, treatment, etc.)
* **cell_counts**: immune cell population counts

### Rationale

* Reduces redundancy. For example:

  * **subjects** table has one row per subject instead of multiple rows in the original csv.

* Improves scalability. For example: 

  * additional samples from existing subjects can be added to **samples** without altering **subjects**, and new experimental analysis for existing samples can be added without altering **samples**.

---

## Code Structure

* **load_data.py**
  Initializes the database schema and loads data from `cell-count.csv`.

* **analysis.py**
  Performs:

  * summary table generation (relative frequencies)
  * statistical analysis (responders vs non-responders)
  * visualization (boxplots)
  * subset queries (Part 4)

  Statistical methods:

  * Welch’s t-test (when n > 30)
  * Mann–Whitney U test (smaller sample sizes)

* **dashboard.py**
  Interactive dashboard for exploring results.

---

## Analysis Summary

### Part 2: Data Overview

* Calculates total cell counts per sample
* Computes relative frequencies (%) for each population

### Part 3: Statistical Analysis

* Compares PBMC samples between responders and non-responders
* Identifies statistically significant differences
* Visualizes distributions using boxplots

### Part 4: Subset Analysis

* Filters melanoma PBMC baseline samples (time = 0, miraclib-treated)
* Reports:

  * samples per project
  * responder vs non-responder counts
  * gender distribution
  * average B cell count for male responders

---

## Key Findings

* CD4 T cell frequencies differed significantly between responders and non-responders (p < 0.05). However, the absolute difference (~0.6%) is small and may not be biologically meaningful


---

## Author

Helena Hopson
